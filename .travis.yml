language: python
python:
  - '3.6'

services:
  - docker

install: skip

stages:
  - test
  - deploy-to-heroku

jobs:
  include:
    - stage: test
      script:
        - docker-compose up -d
        - docker exec climbbeta-web-container python manage.py test
        - docker-compose down
    - stage: deploy-to-heroku
      deploy:
        provider: heroku
        api_key: ${HEROKU_API_TOKEN}
        app:
          master: develop-climbbeta
